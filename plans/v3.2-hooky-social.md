# v3.2 — The Social Loop + Battle Spectacle (response to founder playtest)

Verdicts from playtest: battles suck · believe (💛) does nothing visible · many dead buttons · no countdown · no scribbit detail view · no multiplayer hook · overall sucks. All addressed below.

## 1. THE MULTIPLAYER HOOK — "Back a Scribbit" (new core social loop)

Every day you get ONE Back (a bet) to place on another Redditor's contender in
tonight's Rumble.

- Rumble resolves → backers of the champion earn **Clout** (+3 win, +1 finalist). Clout is permanent, on the leaderboard and flair: the "talent scout" ladder.
- Why it's hooky: your bet forces a return visit; browsing today's entrants (gallery) is daily content made of player drawings; comments become handicapping ("moss chonker counters the forecast, easy money").
- Countdown everywhere: "⚔️ Rumble resolves in 3h 12m" (ArenaState.rumbleResolvesAt).

## 2. BATTLE SPECTACLE + PHASER INNOVATION (the $5K story)

- **Drawings come alive**: WebGL maps the player PNG onto Phaser 4.2 Mesh2D
  geometry (25 vertices, static optimized topology); Canvas uses the proven 3x3
  slice fallback. Idle breathing, birth unfolding, lunge stretch, impact ripple,
  and KO folding all deform the submitted pixels.
- **First-draw promise**: an optional deterministic Doodle Dare removes blank-page
  paralysis, while blank/forming/ready feedback reveals Shape Power only after a
  real body exists. The server repeats the minimum-body check before persistence.
- **Motion DNA**: the dominant analyzed stat selects a named Shape Power —
  INKQUAKE, NIB HALO, SMEARSTEP, or COLORBURST — so drawing decisions control
  both combat numbers and visible motion. Element gives each power one of sixteen
  authored signature identities; transcript-derived dodge, dead-zone, Halo Guard,
  and element stamps make the existing counterplay readable.
- **Server-authored live spectacle**: a deterministic 20 Hz simulation resolves
  the winner and stores a bounded timeline plus motion checkpoints. Phaser
  interpolates the transcript as continuous wall-bouncing action, so replay is
  cheap, cheat-resistant, and does not require realtime infrastructure.
- **Paper sports broadcast**: one expanded arena replaces fixed podiums. Large
  drawings move over attached shadows beneath mirrored HP bars, a fixed-tick
  clock, compact controls, and an Inkcast ticker. Founding NPCs use deterministic
  stat-shaped mascots, so their silhouette previews the same server-selected
  Shape Power without pretending missing player art is authoritative.
- Cinematic pass: camera shake on hits, slow motion on the final fold,
  element-particle move FX, and explicit speed/mute controls. The later
  continuous-replay pass removed TAP-TO-CHEER because fake agency weakened the
  server-authored combat model.
- Deterministic finish: an Inkcast Recap renders the validated transcript's
  verdict, final HP, winner damage, signature, and real decisive hit. KO folds
  only the loser, double KO folds both, and time decisions leave both standing.
- Every owned exhibition has a next fight: win and loss cards can open a
  three-founder Rival Draft. The server authors a stable, level-bounded,
  Shape-Power-varied slate, validates the chosen founder again, and then creates
  a fresh transcript. Cards disclose real builds and forecast status, never win
  odds. “Back someone tonight” remains the social CTA.

## 3. SCRIBBIT DETAIL MODAL

Tap any scribbit anywhere → sticker-card modal: big art, name/artist, level+XP bar, mood, stats 2x2, W/L, belief + backers, days-left pips, battle history (tap → replay), actions contextual: care/spar/enter (mine) · believe/back (others').

## 3.5 PERMANENT COLLECTION LOOP

- Daily Ink Trail makes the existing reward economy obvious: draw +2 · each care +1 · first spar win +2, with the first capsule each UTC day discounted to 5 Ink.
- Capsule discovery survives consumed accessory copies and fading Scribbits. Pens, titles, collector rank, and the album are permanent account progress.
- Show the exact collection count and pulls remaining until guaranteed Epic before spending. Gacha is earned-only and cosmetic; drawing remains combat authority.
- The 36-item Collection includes eight cosmetic Shape Power Relics, two per
  power, so a pull can reinforce battle identity without becoming hidden power.

## 4. DEAD-BUTTON AUDIT (zero tolerance)

Local mock harness (scripts/dev-mock.mjs: serves dist/client + stubs all /api/\* with realistic fixtures) → orchestrator click-sweeps EVERY interactive element headlessly and files/fixes. Believe button specifically: visible +1 float animation, optimistic update, disabled after use, error surfaced.

## Contract additions (orchestrator-owned, done)

ArenaState += rumbleResolvesAt, todayEntrants[], myBackedScribbitId, myClout
POST /api/back {scribbitId} (once/day, locks at resolve) · GET /api/clout-board
Clout payout in nightly job. Belief unchanged (affection ≠ betting).

## Split

- Codex: mock harness · back/clout/payouts · todayEntrants + rumbleResolvesAt · clout board · believe returns fresh count. Tests for payout math.
- Opus: mesh-deform fighter rendering · cinematic replay · detail modal · entrants gallery + Back UI + countdown · loss flow · fix every dead button (harness-verified).
- Orchestrator: contract (done) · click-sweep QC on harness · screenshots to user.
