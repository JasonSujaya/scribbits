Findings

- [src/server/routes/api.ts:324](/Users/jasons/Github/Hackathon/Remonsta/app/src/server/routes/api.ts:324) — CRITICAL — Same active `spawnId` can be submitted repeatedly to inflate personal/global catch counts.  
  Fix: gate awards with Redis idempotency per `userId + spawnId` before `awardCatch`.

- [src/server/routes/api.ts:257](/Users/jasons/Github/Hackathon/Remonsta/app/src/server/routes/api.ts:257) — CRITICAL — Public catch params plus client-supplied `tapTimesMs` let anyone synthesize perfect catches without playing.  
  Fix: issue a one-time attempt token/start time server-side and require it on `/api/catch`.

- [src/client/scenes/CatchMinigame.ts:204](/Users/jasons/Github/Hackathon/Remonsta/app/src/client/scenes/CatchMinigame.ts:204) — HIGH — Client judges hits with float time but submits rounded time, so boundary taps can pass locally and fail server replay.  
  Fix: use the exact submitted timestamp for local radius checks, ideally via shared catch math.

- [src/client/scenes/CatchMinigame.ts:201](/Users/jasons/Github/Hackathon/Remonsta/app/src/client/scenes/CatchMinigame.ts:201) — HIGH — Client allows unlimited taps, but server silently forces failure at `>5` taps.  
  Fix: enforce the same max tap count in the minigame UI or remove/change the server cap.

- [src/client/scenes/Habitat.ts:305](/Users/jasons/Github/Hackathon/Remonsta/app/src/client/scenes/Habitat.ts:305) — MEDIUM — Logged-out users can play the full catch flow, then only hit a 401 after submission.  
  Fix: expose auth state before catch and call `showLoginPrompt()` instead of starting the minigame.

- [devvit.json:37](/Users/jasons/Github/Hackathon/Remonsta/app/devvit.json:37) — MEDIUM — Menu registers `/internal/menu/example-form`, but server only implements `/post-create`, so that menu action 404s.  
  Fix: remove the sample menu item or add `menu.post('/example-form')`.

Verified: spawn UTC/common/expiresAt logic looked correct; Redis calls match local `@devvit/web`/`@devvit/redis` d.ts. `npm run type-check` and `npm run lint` pass. `npm run build` was blocked by read-only sandbox writing `node_modules/.vite-temp`.