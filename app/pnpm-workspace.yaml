packages:
  - .

# protobufjs@7.6.3 only runs a local version-scheme compatibility check in its
# postinstall script. It is an indirect Devvit dependency and is intentionally
# the sole allowed dependency build.
allowBuilds:
  protobufjs: true
